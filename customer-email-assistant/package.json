{
  "name": "customer-email-assistant",
  "version": "1.0.0",
  "description": "AI-powered customer email assistant with automatic classification and reply generation",
  "scripts": {
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "client": "cd client && npm start",
    "server": "cd server && python main.py",
    "install-all": "npm install && cd client && npm install && cd ../server && pip install -r requirements.txt",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "keywords": ["email", "ai", "customer-service", "automation"],
  "author": "",
  "license": "MIT"
}